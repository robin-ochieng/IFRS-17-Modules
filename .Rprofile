# .Rprofile
# This file ensures that R uses the CRAN repository for package installation
options(repos = c(CRAN = "https://cran.rstudio.com"))

# Optional: Set other R options
options(stringsAsFactors = FALSE)
options(scipen = 999)  # 